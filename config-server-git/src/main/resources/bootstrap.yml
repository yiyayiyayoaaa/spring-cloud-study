spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yiyayiyayoaaa/spring-cloud-config-repository/
          username: *********
          password: *****
server:
  port: 1201
